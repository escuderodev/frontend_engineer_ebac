// Exercício 1

// declaração de variáveis
let altura = 6;
let base = 6;

// função para calcular a área
function calculaArea(altura, largura) {
    return altura * largura;
};

// evocando a função
console.log(`A Área atual é igual à ${calculaArea(altura,base).toFixed(1)} M².`);